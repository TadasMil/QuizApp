import React, { Component } from 'react'

export default class AppWrapper extends Component {
    state = {
        auth: false,
    }
    
    render() {
        return (
            <h1>tadas</h1>
        )
    }
}

